package com.snowy.travsalesman.geneticalg;

/**
 * Vector2 (x, y) add, subs, distance
 * City - Name, Position, calculateDistance(city1, city2) => distance
 * Route - permutate(index1, index2), length()
 * generateRandomRoutes(number of routes, cities, initial)
 * 
 * loadCities(filename)
 * <CITY_NAME> <X> <Y>
 */
public final class App {
    public static void main(String[] args) {
        System.out.println("Hello World!");
    }
}
